#!/usr/bin/zsh


# link scripts
ln -s `pwd`/upd.sh '/Apps/upd' # --force

ln -s `pwd`/prtsc.sh '/Apps/prtsc' # --force
ln -s `pwd`/opti.sh '/Apps/opti' # --force

ln -s `pwd`/kk.sh '/Apps/kk' # --force
ln -s `pwd`/purge.sh '/Apps/purge' # --force

ln -s `pwd`/restart.sh '/Apps/restart' # --force
ln -s `pwd`/off.sh '/Apps/off' # --force
